export const error_message = ((message:string):object => {
    return {'errorMessage': message}
})

export const success_message = ((message:string):object=>{
    return {'successMessage': message}
})

